
# server:
#   port: 8080

# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/starbucks_db
#     username: root
#     password: 1234
#     driver-class-name: com.mysql.cj.jdbc.Driver
#   jpa:
#     hibernate:
#       ddl-auto: update  # create, create-drop, update, validate, none
#     show-sql: true
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.MySQL8Dialect
#         format_sql: true

#jwt:
#  secret-key:
#  assess-expire-time:


spring:
  datasource:
    url: jdbc:mysql://${{secrets.EC2_HOST}}/starbucks-db
    username: ${{secrets.DB_USER}}
    password: ${{secrets.DB_PWD}}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    jdbc:
      dialect: mysql

